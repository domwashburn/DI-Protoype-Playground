/**
 * Carbon Design System v11 Components — Phase 4C complete
 *
 * All components are now re-exported directly from @carbon/react.
 * ButtonAdapter has been deleted — consumers import Button from @carbon/react.
 * OverflowMenu family → @carbon/react (Phase 3C complete).
 * Breadcrumb + BreadcrumbItem added Phase 4C.
 */

// Button — direct from @carbon/react (ButtonAdapter deleted Phase 3A)
export { Button } from '@carbon/react';
export type { ButtonProps } from '@carbon/react';

// OverflowMenu — re-export from @carbon/react (Phase 3C)
export { OverflowMenu, OverflowMenuItem } from '@carbon/react';

// Breadcrumb — re-export from @carbon/react (Phase 4C)
export { Breadcrumb, BreadcrumbItem } from '@carbon/react';