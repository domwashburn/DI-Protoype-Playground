/**
 * Modal Component Family — Phase 3B
 *
 * Modal → @carbon/react ComposedModal via ModalAdapter.
 * DisplaySettingsContent → unchanged (content-only, no shell dependency).
 */

export { default as Modal } from './ModalAdapter';
export { default } from './ModalAdapter';
export { DisplaySettingsContent } from './DisplaySettingsContent';
