/**
 * Core Hooks
 * 
 * Shared hooks used across all editors (BAL, Formula, Markdown, etc.).
 * Includes autocomplete triggers, syntax highlighting, validation, etc.
 */

// Autocomplete system
export * from './useAutocompleteTriggers';